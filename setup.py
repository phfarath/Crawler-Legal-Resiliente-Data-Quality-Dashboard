from setuptools import find_packages, setup

setup(
    name="crawler-legal-resiliente",
    version="1.0.0",
    description="Crawler resiliente para decisões judiciais",
    author="Crawler Legal Team",
    packages=find_packages(),
    install_requires=[],
)
